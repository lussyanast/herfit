export interface DealsProps {
  image?: string;
  title: string;
  slug: string;
  price: number;
  capacity: number;
}

export interface CategoriesProps {
  icon: string;
  title: string;
  subtitle: string;
}
